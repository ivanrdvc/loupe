// Manual tool labels keyed by tool id (`serverId:toolName`). In code, not DB:
// fork-specific curation, small enough not to earn a table. Empty in core.
export const TOOL_TAGS: Record<string, string[]> = {}
